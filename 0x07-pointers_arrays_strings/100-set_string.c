#include "main.h"

/**
* set_string - sets the value of a pointer to a char
* @s: the initial pointer
* @to: the char returned
* Return: no return value
*/

void set_string(char **s, char *to)
{
	*s = to;
}
